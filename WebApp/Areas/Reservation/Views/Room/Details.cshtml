@using Radyn.Web.Mvc;
@model Room
@{
    ViewBag.Title = Resources.Common.Detail;
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section Head{}
@section PageTitle{@Resources.Common.Detail}
@section PageAction {
    <div class="button-div m-btn waves-red i-return">
        @{Html.Radyn().ImageActionLink().NavigateUrl(Url.Content("~/Reservation/Room/Index?floorId="+Model.FloorId)).Text(Resources.Common.Return).Align(ImageActionLink.Alignment.Left).ImageUrl(Url.Content("~/Content/Images/go-back.png")).ImagePosition(ImageActionLink.Position.Right).Render();}
    </div>
}
@section PageActionDown {
    <div class="button-div m-btn waves-red i-return">
        @{Html.Radyn().ImageActionLink().NavigateUrl(Url.Content("~/Reservation/Room/Index?floorId=" + Model.FloorId)).Text(Resources.Common.Return).Align(ImageActionLink.Alignment.Left).ImageUrl(Url.Content("~/Content/Images/go-back.png")).ImagePosition(ImageActionLink.Position.Right).Render();}
    </div>
}
@if (Model != null)
{
    @Html.ValidationSummary(true)
    <div class="radynForm">
        <div>
            <div class="table-row-cap">
                <div class="item">
                    @Resources.Reservation.FloorName
                </div>
            </div>
            <div class="table-row-item drp">
                <div class="item">
                    @Html.DisplayFor(x => x.HotelFloor.Name)
                </div>
            </div>
        </div>
        <div>
            <div class="table-row-cap">
                <div class="item">
                    @Resources.Reservation.Title
                </div>
            </div>
            <div class="table-row-item drp">
                <div class="item">
                    @Html.DisplayFor(model => model.Title)
                </div>
            </div>
        </div>
        <div>
            <div class="table-row-cap">
                <div class="item">
                    @Resources.Reservation.RoomType
                </div>
            </div>
            <div class="table-row-item drp">
                <div class="item">
                    @Html.DisplayFor(model => model.RoomType.Title)
                </div>
            </div>
        </div>

    </div>
}
